# Repo conventions

## Git workflow
- Commit and push directly to `main` for all work in this repo. Do not create feature branches or open PRs unless the user explicitly asks. (User authorized this on 2026-05-11, overriding the default "develop on a feature branch" policy.)
- GitHub Pages serves from `main`, so commits to `main` go live immediately.
